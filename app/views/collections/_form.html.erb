<%= form_with(model: collection, local: true) do |form| %>
  <% if collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collection.errors.count, "error") %> prohibited this collection from being saved:</h2>

      <ul>
      <% collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :artist %>
    <%= form.text_field :artist, id: :collection_artist %>
  </div>

  <div class="field">
    <%= form.label :album %>
    <%= form.text_field :album, id: :collection_album %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :collection_year %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_field :img, id: :collection_img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
